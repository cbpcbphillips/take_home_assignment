services:
  db:
    image: postgres:18
    environment:
      POSTGRES_USER: atlas
      POSTGRES_PASSWORD: atlas
      POSTGRES_DB: atlas
    ports:
      - "5432:5432"
    volumes:
      - atlas_data:/var/lib/postgresql

volumes:
  atlas_data:
